import '../../styles/index.scss';
const React = require('react');

const WeatherApp = (props) => {

    return (
        <div>
            <h1>Weather App</h1>
        </div>
    );
}

export default WeatherApp;